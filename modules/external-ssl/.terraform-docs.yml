version: ">= 0.13, < 1.0.0"

formatter: markdown table
header-from: ../../examples/external-ssl/example.tf
sections:
  show:
    - header
    - inputs
    - resources
    - modules
    - outputs

output:
  file: README.md
  mode: inject

settings:
  indent: 4
  escape: false
  default: true
  required: true
  type: true

sort:
  enabled: true
  by: required
